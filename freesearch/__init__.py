"""Vendored subset of the Braudit engine (see vendor_engine.py).

Only the deterministic class/term modules are here — NOT the live-search stack,
so importing this package is cheap and has no Temmy dependency.
"""
